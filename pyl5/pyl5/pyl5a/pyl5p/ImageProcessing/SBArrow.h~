#pragma once
#include "SBDefinitions.h"

class SBArrow
{
	Path path;
	int label;

public:
	SBArrow(void);
	~SBArrow(void);

	Path GetPath();
	void SetPath(Path pPath);

	int GetLabel();
	void SetLabel(int pLabel);

};

